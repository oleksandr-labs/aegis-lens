{
  "name": "@ua-map/milbloggers",
  "version": "1.0.0",
  "description": "Editorially curated milblogger / OSINT allow-list — side-labelled ingest, cross-reference, misinfo flagging, narrative comparison.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
