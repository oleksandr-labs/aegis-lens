{
  "name": "@ua-map/un-ocha",
  "version": "1.0.0",
  "description": "UN OCHA + ReliefWeb humanitarian ingestion — HDX datasets, ReliefWeb situation reports, IOM DTM displacement, per-cluster reports. PII strict redaction (fail-closed).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
