{
  "name": "@ua-map/notebooks",
  "version": "1.0.0",
  "description": "Analyst notebooks — typed cell model for markdown, query, map, chart, AI, and code cells.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
