{
  "name": "@ua-map/rule-builder",
  "version": "1.0.0",
  "description": "AI-powered rule builder: NL → structured alert rule JSON with preview + dry-run.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
