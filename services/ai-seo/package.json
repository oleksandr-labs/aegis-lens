{
  "name": "@ua-map/ai-seo",
  "version": "1.0.0",
  "description": "AI-assisted SEO asset generation — metadata, FAQs, intros, internal links, schema.org, alt-text, per-locale variants — behind hallucination/quality + editorial gates.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
