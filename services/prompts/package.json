{
  "name": "@ua-map/prompts",
  "version": "1.0.0",
  "description": "Central prompt library: versioned, typed, locale-aware prompts for all AI services.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
