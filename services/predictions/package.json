{
  "name": "@ua-map/predictions",
  "version": "1.0.0",
  "description": "AI forecasting governance: backtest schema, point-prediction policy guard, feedback eval, persona gating. Directional-only — never confused with observation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
