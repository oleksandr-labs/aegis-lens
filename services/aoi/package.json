{
  "name": "@ua-map/aoi",
  "version": "1.0.0",
  "description": "Area of Interest monitoring service — geometry, point-in-AOI matching, change detection scheduling.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc", "test": "vitest run" },
  "dependencies": {
    "@ua-map/schema": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": { "typescript": "^5.4.5", "vitest": "^1.6.0" }
}
