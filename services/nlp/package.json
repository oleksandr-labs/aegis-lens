{
  "name": "@ua-map/nlp",
  "version": "1.0.0",
  "description": "NLP service — language detection, translation, NER, classification, summarization, embeddings.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@ua-map/schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
