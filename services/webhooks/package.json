{
  "name": "@ua-map/webhooks",
  "version": "1.0.0",
  "description": "Reliable, signed, replayable webhook delivery with HMAC-SHA256 and exponential backoff.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
