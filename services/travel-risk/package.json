{
  "name": "@ua-map/travel-risk",
  "version": "1.0.0",
  "description": "Per-city and per-route risk scoring with composite index for travel safety briefings.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
