{
  "name": "@ua-map/flags",
  "version": "1.0.0",
  "description": "Feature flags: per-org/user/cohort targeting, kill switches, percentage rollouts.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
