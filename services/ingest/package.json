{
  "name": "@ua-map/ingest",
  "version": "1.0.0",
  "description": "Ingest service — pull/push from sources, dedup, archive, normalise.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@ua-map/schema": "workspace:*",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
