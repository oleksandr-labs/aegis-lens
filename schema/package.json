{
  "name": "@ua-map/schema",
  "version": "1.0.0",
  "description": "Canonical event schema and taxonomy for the Ukrainian conflict intelligence platform.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
